At most times domain modules will work on one database and will use same kind of models. 
Imo it would be easier to maintain one dir of models than drop them to concrete modules. But this
is optional, when we will support resources per module, do export to one directory will not be hard.